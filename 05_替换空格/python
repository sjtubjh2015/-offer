class Solution(object):
    def replaceSpace(self, s):
        """
        :type s: str
        :rtype: str
        """

        ans = ""
        for char in s:
            if char == ' ':
                ans += "%20"
            else:
                ans += char
        return ans
